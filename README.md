# web_lab_Project

This is a Web Lab project featuring a Node.js backend server.

## Getting Started

1. Navigate to the `server` directory.
2. Run `npm install` to install dependencies.
3. Configure the `.env` file with your MongoDB URI.
4. Start the server using `npm start` or `node index.js`.
